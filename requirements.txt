# Jarvis offline ears — wake word + VAD + STT.
# Tested on Python 3.11. Install:  pip install -r requirements.txt
numpy>=1.26
requests>=2.31
sounddevice>=0.4.6
torch>=2.2
faster-whisper>=1.0.3
openwakeword>=0.6.0
silero-vad>=5.1
