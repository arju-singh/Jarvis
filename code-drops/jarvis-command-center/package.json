{
  "name": "jarvis-command-center",
  "private": true,
  "type": "module",
  "version": "1.0.0",
  "description": "Real-time AI agent command center — reactive HUD + SSE event bus + pluggable LLM brain with automatic failover. Runs with zero API keys.",
  "scripts": {
    "dev": "node --import tsx --env-file-if-exists=.env server.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "express": "^5.1.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^22.0.0",
    "tsx": "^4.20.0",
    "typescript": "^5.7.0"
  }
}
